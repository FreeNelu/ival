import { StyleSheet } from "react-native";

const styles = StyleSheet.create({
  Card: {
    backgroundColor: "transparent",
    borderWidth: 0,
  },
  Image: { opacity: 0.8, borderRadius: 24 },
  Title: { color: "white", fontSize: 42 },
});

export default styles;
